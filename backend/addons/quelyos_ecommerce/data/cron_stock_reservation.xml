<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <!-- Sequence for Stock Reservation -->
        <record id="seq_stock_reservation" model="ir.sequence">
            <field name="name">Stock Reservation</field>
            <field name="code">stock.reservation</field>
            <field name="prefix">SR</field>
            <field name="padding">5</field>
            <field name="number_next">1</field>
            <field name="number_increment">1</field>
        </record>

        <!-- Cron Job: Expire Old Stock Reservations -->
        <record id="cron_expire_stock_reservations" model="ir.cron">
            <field name="name">E-commerce: Expire Stock Reservations</field>
            <field name="model_id" ref="model_stock_reservation"/>
            <field name="state">code</field>
            <field name="code">model._cron_expire_reservations()</field>
            <field name="interval_number">5</field>
            <field name="interval_type">minutes</field>
            <field name="numbercall">-1</field>
            <field name="active" eval="True"/>
            <field name="doall" eval="False"/>
        </record>

        <!-- Cron Job: Cleanup Old Stock Reservations -->
        <record id="cron_cleanup_stock_reservations" model="ir.cron">
            <field name="name">E-commerce: Cleanup Old Stock Reservations</field>
            <field name="model_id" ref="model_stock_reservation"/>
            <field name="state">code</field>
            <field name="code">model._cron_cleanup_old_reservations()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="numbercall">-1</field>
            <field name="active" eval="True"/>
            <field name="doall" eval="False"/>
        </record>

    </data>
</odoo>
