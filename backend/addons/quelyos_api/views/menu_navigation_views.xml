<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- List View -->
    <record id="view_menu_navigation_list" model="ir.ui.view">
        <field name="name">quelyos.menu.list</field>
        <field name="model">quelyos.menu</field>
        <field name="arch" type="xml">
            <list string="Menus Navigation">
                <field name="sequence" widget="handle"/>
                <field name="code"/>
                <field name="name"/>
                <field name="label"/>
                <field name="url"/>
                <field name="parent_id"/>
                <field name="active" widget="boolean_toggle"/>
            </list>
        </field>
    </record>

    <!-- Form View -->
    <record id="view_menu_navigation_form" model="ir.ui.view">
        <field name="name">quelyos.menu.form</field>
        <field name="model">quelyos.menu</field>
        <field name="arch" type="xml">
            <form string="Menu Navigation">
                <sheet>
                    <div class="oe_title">
                        <h1>
                            <field name="label" placeholder="Libellé du menu..."/>
                        </h1>
                    </div>
                    <group>
                        <group>
                            <field name="name"/>
                            <field name="code"/>
                            <field name="url"/>
                            <field name="icon"/>
                        </group>
                        <group>
                            <field name="parent_id"/>
                            <field name="sequence"/>
                            <field name="active"/>
                            <field name="open_new_tab"/>
                            <field name="requires_auth"/>
                        </group>
                    </group>
                    <group string="Options avancées">
                        <field name="description"/>
                        <field name="css_class"/>
                    </group>
                    <notebook>
                        <page string="Sous-menus" invisible="parent_id">
                            <field name="child_ids">
                                <list>
                                    <field name="sequence" widget="handle"/>
                                    <field name="label"/>
                                    <field name="url"/>
                                    <field name="active" widget="boolean_toggle"/>
                                </list>
                            </field>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Action -->
    <record id="action_menu_navigation" model="ir.actions.act_window">
        <field name="name">Menus Navigation</field>
        <field name="res_model">quelyos.menu</field>
        <field name="view_mode">list,form</field>
    </record>

    <!-- Menu Item -->
    <menuitem
        id="menu_quelyos_cms_navigation"
        name="Menus Navigation"
        parent="menu_quelyos_cms"
        action="action_menu_navigation"
        sequence="60"/>
</odoo>
