{
  "name": "@quelyos/backend",
  "version": "1.0.0",
  "description": "Unified backend client for Quelyos Suite (Next.js SSR, Client, Vite)",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "axios": "^1.13.2",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^25.2.0",
    "typescript": "^5"
  },
  "peerDependencies": {
    "zod": "^4.0.0"
  }
}
