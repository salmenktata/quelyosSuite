# Ports Quelyos Suite

## 🔒 PORTS FIXES - NE JAMAIS MODIFIER

| Service | Port | Projet | Config |
|---------|------|--------|--------|
| **Backend Odoo** | 8069 | `odoo-backend/` | `docker-compose.yml` |
| **Site Vitrine** | 3000 | `vitrine-quelyos/` | `next.config.js` |
| **E-commerce** | 3001 | `vitrine-client/` | `next.config.js` |
| **Backoffice** | 5175 | `dashboard-client/` | `vite.config.ts` |
| **Super Admin** | 5176 | `super-admin-client/` | `vite.config.ts` |
| **PostgreSQL** | 5432 | Docker | `docker-compose.yml` |
| **Redis** | 6379 | Docker | `docker-compose.yml` |

## ⚠️ Règle Absolue

**INTERDICTION de modifier ces ports** dans :
- `vite.config.ts`
- `next.config.js`
- `docker-compose.yml`
- Scripts dev (`dev-start.sh`, `dev-stop.sh`)

## 🔍 Vérification

```bash
# Vérifier les ports configurés
grep "port:" dashboard-client/vite.config.ts
grep "port:" super-admin-client/vite.config.ts

# Vérifier les processus actifs
lsof -i :5175 -i :5176 | grep LISTEN
```

## 🚀 Démarrage Sécurisé

Le script `dev-start.sh` nettoie automatiquement les ports avant démarrage :

```bash
./scripts/dev-start.sh all        # Tous les services
./scripts/dev-start.sh backoffice # Port 5175
./scripts/dev-start.sh superadmin # Port 5176
```

## 🛠️ En Cas de Conflit

Si un port est occupé par le mauvais service :

```bash
# Tuer le processus zombie
lsof -ti:5176 | xargs kill -9

# Redémarrer proprement
./scripts/dev-start.sh superadmin
```

---

**Dernière mise à jour** : 2026-01-29
